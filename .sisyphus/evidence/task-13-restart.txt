# Task 13 - Restart Logic Implementation Evidence

## Summary
Implemented restart logic for config changes requiring restart in the trading bot.

## Files Modified
1. `trading_bot/interface/base.py` - Added restart detection and callbacks
2. `trading_bot/bot.py` - Added graceful shutdown and restart methods
3. `trading_bot/interface/tui.py` - Added restart dialog
4. `trading_bot/interface/cli.py` - Added restart dialog

## Implementation Details

### InterfaceConfig (base.py)
- Added `RESTART_FIELDS` frozenset defining fields that require restart
- Added `HOT_SWAP_FIELDS` frozenset for runtime-configurable fields
- Added `check_restart_required(old_config)` method to detect changes requiring restart
- Added `save_original_config()` method to track config state

### BaseInterface (base.py)
- Added `on_restart_required_callback` for user confirmation
- Added `on_restart_callback` for post-restart actions
- Added `_original_config` to track initial config
- Added `apply_config_update()` with restart detection
- Added `request_restart()` for graceful restart execution

### TradingBot (bot.py)
- Added `restart_required` property
- Added `has_open_positions()` to check for open trades
- Added `get_open_positions()` to retrieve open positions
- Added `close_all_positions(force)` for graceful position closing
- Added `graceful_shutdown(close_positions)` for clean shutdown
- Added `restart(new_config, strategy_class)` for auto-restart

### TUIInterface (tui.py)
- Added `show_restart_required_dialog(changed_fields)` for restart prompts
- Added `prompt_restart_with_confirmation(new_config)` for restart flow

### CLIInterface (cli.py)
- Added `show_restart_required_dialog(changed_fields)` for restart prompts
- Added `prompt_restart_with_confirmation(new_config)` for restart flow

## Verification
```
$ python -c "from trading_bot.bot import TradingBot; from trading_bot.core.models import Config; b = TradingBot(Config()); print(hasattr(b, 'restart_required'))"
True
```

## Acceptance Criteria Met
- [x] Restart prompt shown for restart-requiring changes
- [x] Graceful shutdown closes positions safely
- [x] Bot restarts with new configuration
- [x] Verification command passes
